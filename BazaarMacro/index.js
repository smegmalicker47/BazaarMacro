import Config from './config'

//this is NOT wh
//okay so lets see here
//this is an index file so it's good and important
//time to code
// * stretches fingers



register('command', () => {
    Config().getConfig().openGui()

}).setName("bam");